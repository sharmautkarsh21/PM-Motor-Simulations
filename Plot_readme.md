## Setup

Install the required packages:

```powershell
pip install -r requirements_dash.txt
```

Or install individually:

```powershell
pip install dash plotly numpy
```

## What happens:

1. A web server starts on port 8050
2. Your default browser opens to `http://127.0.0.1:8050`


